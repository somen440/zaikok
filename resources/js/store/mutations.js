export default {
  SET_IS_GUEST: (state, is) => {
    state.isGuest = is
  },

  SET_TOKEN: (state, token) => {
    state.token = token
  },

  SET_USER: (state, user) => {
    state.user = user
  },
}
